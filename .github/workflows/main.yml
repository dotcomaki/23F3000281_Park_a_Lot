name: Email Action

on:
  workflow_dispatch:   # lets you trigger it manually from the Actions tab
  push:                # also runs on every push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f3000281@ds.study.iitm.ac.in
        run: echo "Hello, world!"
